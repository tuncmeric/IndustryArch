node_modules
# dist/ is committed because cPanel's Git Version Control deploy
# environment doesn't run npm — we ship pre-built bundles to the server.
dist-ssr
*.local
*.log

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.sw?

# Build / deploy artifacts (regenerated locally before each deploy)
deploy.tar.gz
deploy.zip
api-update.tar.gz
smtp-test.tar.gz

# Local PHP secrets (never commit — each environment has its own)
api/config.php
api/contact/test-smtp.php

# User-uploaded files (created at runtime in production)
uploads/projects
uploads/settings

# Lovable cache (legacy)
.lovable
.env
